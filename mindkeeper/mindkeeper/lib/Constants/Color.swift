import SwiftUI

extension Color {
    static let backgroundPrimary = Color.white
    static let backgroundSecond = Color.blue
    static let foregroundPrimary = Color.blue
    static let foregroundSecond = Color.white
    static let foregroundThird = Color.black
}

